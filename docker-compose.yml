version: "3"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=$PORT
    volumes:
      - ./:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port $PORT --reload
